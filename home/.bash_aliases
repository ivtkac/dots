# General aliases
alias ls="$(get_command eza ls) --color=auto"
alias la="ls -a"
alias l='ls -l'
alias ll='ls -l'
alias l.='ls -d .*'

alias cat="$(get_command bat cat)"

# Directory navigation
alias cd="$(get_command z cd)"
