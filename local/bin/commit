#!/bin/bash

git add .

if [ -f /usr/bin/flatpak-spawn ]; then
  export GIT_EDITOR="flatpak-spawn --host flatpak run re.sonny.Commit"
else
  export GIT_EDITOR="flatpak run re.sonny.Commit"
fi

git commit

unset GIT_EDITOR
